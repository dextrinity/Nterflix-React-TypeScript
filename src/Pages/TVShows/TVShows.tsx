import React from "react";

export interface TVShowsProps {}

const TVShows: React.FunctionComponent<TVShowsProps> = (props) => {
  return (
    <div>
     <span className="pageTitle">TV Shows</span>
    </div>
  );
};

export default TVShows;
